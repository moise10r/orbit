# Webhook Module

Implements outbound webhook delivery system with registered endpoints (url, secret, events, active), delivery logs, retry queue (BullMQ), and HMAC-SHA256 signing. CRUD API provided. See entities, service, controller, and processor files for structure and details.